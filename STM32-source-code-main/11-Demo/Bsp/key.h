/**********************************************************
 * 文件名: key.h
 * 作  者: 电子科技协会嵌入式组
 * 日  期: 2025-09-28
 * 
 * 功能说明:
 *   声明按键相关的函数接口，提供按键获取按键键码的功能，支持多个按键的检测
 * 
 * 接口备注:
 *   1. 支持4个按键的状态检测
 *   2. key_getnum()函数返回值为0-4，0表示无按键按下，1-4对应不同按键
 *   3. 按键检测包含包含消抖处理，采用阻塞式操作，需等待按键松手
 **********************************************************/

#ifndef __KEY_H
#define __KEY_H

#include "main.h"

uint8_t key_getnum(void);

#endif
