# ---------------------------------------------------------------------------
# 1. Keil MDK (uVision) 核心编译垃圾 (最占空间的部分)
# ---------------------------------------------------------------------------
*.o
*.obj
*.crf
*.d
*.dep
*.map
*.lst
*.axf
*.htm
*.hex
*.bin
*.sct
*.iex
*.lnp
*.plg
*.rpt
*.tra
*.__i
*.bak

# Keil V5 编译日志
*.build_log.htm

# ---------------------------------------------------------------------------
# 2. 这是 Keil 的"会话文件"，必须忽略！
# ---------------------------------------------------------------------------
# 包含你的窗口布局、打开了哪几个文件、光标在哪一行
*.uvguix.*
*.uvgui.*
*.uvoptx

# ---------------------------------------------------------------------------
# 3. STM32CubeMX 特定文件
# ---------------------------------------------------------------------------
# CubeMX 的内部工程描述文件，包含绝对路径，必须忽略
.mxproject

# CubeMX 备份文件
*.ioc.backup

# ---------------------------------------------------------------------------
# 4. 编辑器与系统垃圾
# ---------------------------------------------------------------------------
.vscode/
Thumbs.db
Desktop.ini

# ---------------------------------------------------------------------------
# 5. 特殊说明
# ---------------------------------------------------------------------------
# J-Link 调试产生的日志
JLinkLog.txt
*.scvd

# RTE (Run-Time Environment) 
# 如果你只用 CubeMX 生成 HAL 库，而不使用 Keil 的 RTE 包管理器，可以忽略此文件夹
RTE/

# 注意：我移除了 DebugConfig/ 的忽略规则
# 建议保留调试器配置文件，以保证在不同电脑上调试行为一致.

# ---------------------------------------------------------------------------
# 6. 百度网盘同步空间临时文件
# ---------------------------------------------------------------------------
*.baiduyun.*
*/.baiduyun/*