# STM32F407VE + ST-LINK/V2 配置文件
# 用于 OpenOCD + Cortex-Debug 调试

source [find interface/stlink.cfg]
transport select dapdirect_swd
adapter speed 4000
source [find target/stm32f4x.cfg]

# 使用系统复位，避免影响外部看门狗
reset_config srst_only srst_nogate
